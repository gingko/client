var localStoreId;
var treeId;
const localStore = {
  db: (tree_id) => {
  },
  isReady: () => {
    return true
  },
  load: () => {
  },
  get: (key, fallback) => {
  },
  set: (key, value) => {
  },
};

export {
  localStore
}
